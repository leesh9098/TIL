```javascript
console.log('%c출력하고 싶은 문자열', 'css속성 값;');
// 개발자 도구의 console창에서 %c뒤에 붙은 문자열에 StyleSheet가 적용된 것을 확인할 수 있다


prompt('문자열 입력');
// alert창에서 사용자의 키보드 입력 값을 받을 수 있다
// ※문자열만 리턴하기 때문에 숫자 계산 같은 것은 불가능


/*
 ParseInt와 Number의 차이
 ParseInt: 입력 받은 문자열 중 첫 번째 문자가 숫자로 변환 가능할 경우, 숫자가 아닌 부분이 나올 때까지 변환한다
           또한 ParseInt('숫자', 진수)를 입력해 자신이 입력한 숫자의 값을 원하는 진수로 변환할 수 있다
 Number: 문자열이 하나라도 포함되면 NaN(Not a Number)를 리턴한다
*/


typeof 변수명;
// 변수나 값의 타입을 알아낼 수 있다
```
